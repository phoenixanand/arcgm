services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: arcgm
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - arcgm_pg:/var/lib/postgresql/data
volumes:
  arcgm_pg:
